VSCode must be opened using "Developer Command Prompt for VS 20XX" in order to compile.

If running for the first time in VSCode enter 'ctrl-shift p' and type "C/C++ Edit Configurations (UI)" in the resulting bar to generate a c_cpp_properties.json file needed for compilation.

Compile with 'ctrl-shift b' or click Terminal -> Run Build Task...